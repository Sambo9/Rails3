<%= form_for(@product) do |f| %>
  <% if @product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
      <% @product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title, :class=>"ui label" %>
    <%= f.text_field :title, :placeholder => "Title" %>
  </div>
  <div class="field">
    <%= f.label :price, :class=>"ui label"  %>
    <%= f.text_field :price, :placeholder => "€" %>
  </div>
  <div class="field">
    <%= f.label :description, :class=>"ui label"  %>
    <%= f.text_area :description, :placeholder => "Description" %>
  </div>
  <div class="field">
    <%= f.label :user_id, :class=>"ui label"  %>
    <%= f.text_field :user_id, :placeholder => "User" %>
  </div>
  <div class="field">
      <%= f.label :labels, :class=>"ui label"  %>
      <%= f.text_field :all_labels, placeholder: "Labels" %>
    </div>
  <br>
  <div class="actions">
    <%= f.submit :class=> "btn btn-primary" %>
  </div>
  <br>
<% end %>
